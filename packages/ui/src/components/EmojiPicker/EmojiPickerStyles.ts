import {createStyles, makeStyles, Theme} from "@material-ui/core/styles";

const EmojiPickerStyles = makeStyles((theme: Theme) =>
  createStyles({
    iconButton: {
      padding: 10,
    },
  }),
);

export default EmojiPickerStyles;
